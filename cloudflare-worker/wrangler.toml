name = "fo4so-api"
main = "worker.js"
compatibility_date = "2025-01-01"

# 通过 wrangler 注入环境变量（不要写进 git）：
#   wrangler secret put GITHUB_TOKEN   -> 你的 GitHub Token（仓库 Contents: 读写）
#   wrangler secret put OWNER          -> wz3723
#   wrangler secret put REPO           -> FO4SO-KNOWLEDGE
# 或在 Cloudflare 控制台 Worker -> Settings -> Variables & Secrets 添加。
